apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: invoice-api
  labels:
    app.kubernetes.io/name: invoice-api
spec:
  hosts:
    - invoice-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: invoice-api
            port:
              number: 80
          weight: 100
