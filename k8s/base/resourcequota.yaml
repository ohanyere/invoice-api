apiVersion: v1
kind: ResourceQuota
metadata:
  name: invoice-api-quota
  labels:
    app.kubernetes.io/name: invoice-api
    finops.ohanyere.io/cost-center: commerce
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"
